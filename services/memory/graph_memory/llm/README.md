# llm module

SciLLM/Chutes-only client wrappers (`client.py`, `provider.py`, `schemas.py`). Provides JSON-safe completions for edge scoring and related LLM-assisted tasks. Requires `CHUTES_API_KEY`, `CHUTES_API_BASE`, `SCILLM_DEFAULT_MODEL`.
